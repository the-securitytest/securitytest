alert(document.cookie);
// Please note that when you are writing JavaScript inside a file, it shouldn’t be enclosed inside a <script> tag. 
// So consider we created a file cookie.js which contains the code to alert the cookie. 
